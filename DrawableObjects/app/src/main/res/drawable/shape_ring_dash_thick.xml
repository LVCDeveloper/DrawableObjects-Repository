<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="ring"
    android:innerRadius="@dimen/shape_ring_radius"
    android:thickness="@dimen/shape_ring_thickness"
    android:useLevel="false">

    <size
        android:width="90dp"
        android:height="90dp"/>

    <solid
        android:color="@color/clear"/>

    <stroke
        android:width="@dimen/shape_stroke_size"
        android:dashWidth="@dimen/shape_stroke_dash"
        android:dashGap="@dimen/shape_stroke_gap"
        android:color="@color/ring_color"/>

</shape>